{% extends '@App/base.html.twig' %}

{% block body %}

    {% for product in products %}
       <p> <a href="{{ path('view_product',{'id':product.id}) }}">{{ product.title }}</a>
           {{ dump(product.category) }} </p>
    {% endfor %}

{% endblock %}